public class MethodsII {
    public static void greet(){

    }

    public static int square(number) {
        return int number;
    }

    public static boolean isAdult(int age) {
        return true;
    }

    public static char firstLetter(String word) {
        return char;
    }

    public static void printName(String name) {
        System.out.println(name);
    }

    public static int sumThree(int a, int b, int c) {
        return a + b + c;
    }

    public static boolean containsZero(int[] nums) {
        return true;
    }

    // 9: return type

    // 10: parameter list

    // 11: no, area is the product of the parameters width and length

    // 12: fahrenheit

    // 13: the method is checking for a true or false

    // 14: the method prints and that is all it has to do. return is only if you need the output stored elsewhere

    public static boolean isPositive(int num) {
        return num > 0;
    }

    public static int subtract(int a, int b) {
        return a - b;
    }

    // 17: int result = square(6);

    // 18: printName("Maya");

    public static int largestValue(int[] nums) {

    }
}